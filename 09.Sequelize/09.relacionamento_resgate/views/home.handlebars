<h1>Olá Sequelize!</h1>
<div class="container">
    {{#each users}}
        <div class="item">
            <h2>{{this.name}}</h2>
            <p>{{this.occupation}}</p>
            <div class="actions">
                <a href="/user/{{this.id}}" class="details-btn">Ver detalhes</a>
                <a href="/user/edit/{{this.id}}" class="edit-btn">Editar</a>
                <form action="/user/delete/{{this.id}}" method="POST">
                    <input type="submit" value="Excluir" class="delete-btn">
                </form>
            </div>
        </div>
    {{/each}}
</div>