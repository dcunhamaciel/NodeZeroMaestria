<h1>Olá Sequelize!</h1>
<div class="container">
    {{#each users}}
        <div class="item">
            <h2>{{this.name}}</h2>
            <p>{{this.occupation}}</p>
            <div class="actions">
                <a href="/user/{{this.id}}">Ver detalhes</a>
            </div>
        </div>
    {{/each}}
</div>