<h1>Lista de Produtos</h1>
{{#each products}}
    <div>
        <h3>{{this.description}}</h3>
        <p>R$ {{this.price}}</p>
        <p><a href="/product/{{this.id}}">Ver mais</a></p>
    </div>
{{/each}}